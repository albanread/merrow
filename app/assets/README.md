Place the Merrow Studio source icon here as:

- `merrow-studio-icon.png`

Use a square PNG at 1024×1024 or larger. The packaging script will convert it into `MerrowStudio.icns` and bundle it automatically.
